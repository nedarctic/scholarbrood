import { oswald } from "@/app/fonts"

export default function TutorialsPage(){
  return (
    <div className={`${oswald.className} min-h-screen flex flex-col items-center justify-center`}>
      <p className="text-black dark:text-light text-md">Coming Soon.</p>
    </div>
  );
}